import 'package:flutter/material.dart';

class AppColors {
  static const Color backgroundChart = Color(0xFF2D4261);
  static const Color highChart = Color(0xFF44C5FC);
  static const Color lowChart = Color(0xFF69EFAF);

  static const Color primary =
      Color(0xFF864AF9); // Màu hồng hơi tím, khá sáng, dùng làm màu chính
  static const Color onPrimary = Color(
      0xFFFFFFFF); // Màu trắng, dùng cho văn bản và icon trên nền màu chính
  static const Color secondary =
      Color(0xFF3B3486); // Màu xanh hơi đậm, dùng làm màu phụ
  static const Color onSecondary =
      Color(0xFFFFFFFF); // Màu trắng, dùng cho văn bản và icon trên nền màu phụ
  static const Color error = Colors.red; // Màu đỏ cho lỗi
  static const Color lightRed = Color(0xFFFF8A8A);
  static const Color onError =
      Colors.white; // Màu trắng cho văn bản trên nền lỗi
  static const Color onSuccess = Colors.green;
  static const Color surface =
      Color(0xFF864AF9); // Color for component surfaces
  static const Color onSurface = Color(
      0xFF332941); // Màu tối nhất, dùng cho văn bản và icon trên bề mặt vàng
  static const Color background =
      Color(0xFFFFE9B1); // Màu vàng nhẹ cho nền của các màn hình/phần tử layout
  static const Color whiteBackground = Color.fromARGB(255, 243, 243, 243);
  static const Color onBackground =
      Color(0xFF332941); // Màu tối nhất cho văn bản trên nền vàng nhẹ
}
